package seedu.goldencompass.command;

public class Executor {
    private final Map<String, Command> map;
    public Executor(Parser, List<>, List<>) {
        map = Map.of(
                "list", new ListCommand(Parser, List<?> )
        );
    }
}
